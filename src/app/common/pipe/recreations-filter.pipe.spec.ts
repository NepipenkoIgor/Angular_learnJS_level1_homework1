import { RecreationsFilterPipe } from './recreations-filter.pipe';

describe('RecreationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecreationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
